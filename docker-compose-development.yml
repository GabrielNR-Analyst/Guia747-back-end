services:
  postgres:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: guia747
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7.4.4
    ports:
      - "6379:6379"

volumes:
  postgres_data:
